server.port=8000

spring.jpa.hibernate.ddl-auto=none

spring.market-datasource.jdbcUrl=jdbc:mysql://${MYSQL_HOST:localhost}:3306/${MYSQL_DATABASE}
spring.market-datasource.username=${MYSQL_USER}
spring.market-datasource.password=${MYSQL_PASSWORD}
spring.market-datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.broker-datasource.jdbcUrl=jdbc:mysql://${MYSQL_HOST:localhost}:3306/broker
spring.broker-datasource.username=${MYSQL_USER}
spring.broker-datasource.password=${MYSQL_PASSWORD}
spring.broker-datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.properties.hibernate.jdbc.time_zone=UTC
# spring.jpa.show-sql=true

ibgw.baseurl=https://192.168.0.178:8000